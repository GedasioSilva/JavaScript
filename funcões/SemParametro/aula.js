//função que não receber valor de entrada mostra oque tem quardado 
// é usada quando temos um valor fixo e é chamada quando precisamos
function retorna_versao(){
    return 1.1;
}

let versao = retorna_versao();

console.log(versao);