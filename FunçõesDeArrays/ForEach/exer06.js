//Complete o código abaixo de modo que todos os itens de "cores" sejam impressos utilizando forEach.

const cores = ["Verde", "Amarelo", "Azul", "Branco"];

function imprimir(item){
    console.log(item);
}

cores.forEach(imprimir);