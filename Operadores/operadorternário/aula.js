let num =10;
res=(!(num%2) ? "par" : "impar");
console.log(res);

/*------------------------------------------------ */
var nota1 = 10;
var nota2 = 9;

var media = (nota1 >= nota2 ? "sim":"não");

console.log(media);

/*------------------------------------------------ */

var salario = 1350;
var aumento = salario * 0.15;

var valor = (aumento > 210 ? "sim":"não");

console.log(valor);

/*------------------------------------------------ */

var primeira = 8;
var segunda = 6;
var media = (primeira + segunda)/2;

var resultado = (media >= 9 ? "sim":"não");
console.log(resultado);